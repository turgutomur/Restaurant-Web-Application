@model MenuItem
@using RestaurantManagementApp.Models

<div class="container mt-5">
    <!-- Header Section -->
    <div class="text-center mb-4">
        <h2 class="display-5" style="color: #4CAF50;">Edit Menu Item</h2>
        <p class="text-muted">Make changes to the menu item below.</p>
    </div>

    <!-- Form Section -->
    <form asp-action="Edit" class="p-4 rounded shadow bg-white">
        <input type="hidden" asp-for="Id" />
        <div class="form-group">
            <label asp-for="Name" class="font-weight-bold">Name</label>
            <input asp-for="Name" class="form-control" placeholder="Enter item name" />
        </div>
        <div class="form-group">
            <label asp-for="Price" class="font-weight-bold">Price</label>
            <input asp-for="Price" type="number" step="0.01" class="form-control" placeholder="Enter item price" />
        </div>
        <div class="form-group">
            <label asp-for="Category" class="font-weight-bold">Category</label>
            <input asp-for="Category" class="form-control" placeholder="Enter item category" />
        </div>
        <div class="text-center mt-4">
            <button type="submit" class="btn btn-lg text-white" style="background-color: #4CAF50;">Save Changes</button>
        </div>
    </form>
</div>
